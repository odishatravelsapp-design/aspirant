# Optional cloud sync (Google login + cross-device backup via Supabase).
# Leave these unset to run the app 100% locally/offline (default).
# Copy to ".env" and fill in from your Supabase project: Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
